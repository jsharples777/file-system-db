import {SearchFilter} from "./SearchTypes";
import {Collection} from "../collection/Collection";

export class SearchProcessor {
    public static searchCollection(collection:Collection, search:SearchFilter):any[] {
        return [];

    }
}
