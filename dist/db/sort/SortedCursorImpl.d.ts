import { SortedCursor } from "../cursor/SortedCursor";
import { CursorImpl } from "../cursor/CursorImpl";
export declare class SortedCursorImpl extends CursorImpl implements SortedCursor {
    constructor(items: any[]);
}
